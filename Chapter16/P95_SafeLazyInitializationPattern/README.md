# Safe-lazy initialization pattern
The _safe-lazy initialization pattern_, also known as the _initialization-on-demand holder_ design pattern, 
allows for the safe publication of an object's state and reference, enabling shared access across threads.
 Exemplify this pattern before and after JDK 16. 