# Introducing `newCondition()` 
Conditions allow one thread to suspend execution ("wait") until it is notified by 
another thread that a specific state condition may now be true. Provide the idiom and a meaningful example.