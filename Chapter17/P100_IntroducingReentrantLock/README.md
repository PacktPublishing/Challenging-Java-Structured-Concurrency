# Introducing the `ReentrantLock` 
If we think about how `synchronized` on steroids will look like, we can consider the `Lock` interface, 
which comes up with a fine-grained control of the locking mechanism. In this context, introduce and exemplify the _reentrant locks_.