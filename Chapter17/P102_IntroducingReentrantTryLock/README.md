# Introducing `tryLock()` 
The `ReentrantLock.tryLock()` and `tryLock(long time, TimeUnit unit)` allow us to attempt acquiring a lock immediately (`tryLock()`) or 
for a specified period of time. Provide the `tryLock()` idiom and exemplify it in code.