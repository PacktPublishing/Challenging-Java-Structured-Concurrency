# Challenge time 
Challenge yourself to write a thread factory (ThreadFactory implementation) that 
returns virtual or platform threads based on a given flag (true for virtual threads, 
false for platform threads). 