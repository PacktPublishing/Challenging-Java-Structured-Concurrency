# Challenge time
Challenge yourself to write a thread factory (`ThreadFactory` implementation) 
that returns virtual or platform threads based on a given flag (`true` for virtual threads, 
`false` for platform threads) where the virtual threads are named `virtualThread-0`, `virtualThread-1`, …, 
and the platform threads are named `platformThread-0`, `platformThread-1`, ….
