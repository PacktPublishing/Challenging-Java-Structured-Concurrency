# Pointing `ThreadLocal` shortcomings
Provide several examples of using thread-local variables (e.g., for modeling a user shopping cart, 
or database connections, or transaction ID management). In addition, highlight the main shortcomings of thread-local variables.