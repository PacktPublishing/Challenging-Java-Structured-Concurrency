# `then…()` versus `then…Async()` callbacks
As you probably noticed in the JDK documentation, `thenApply()` also has a flavor named `thenApplyAsync()`.
 Provide a detailed explanation about the differences between these two methods.